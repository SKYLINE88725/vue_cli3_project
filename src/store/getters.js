let getters={
    bTitle:(state)=>state.bNav,
    bFoot:(state)=>state.bFoot,
    bLoading:(state)=>state.bLoading,
    navIndex:(state)=>state.navIndex,
    newItem:(state)=>state.newItem,
    topName:(state)=>state.topName,
    
  };
  export default getters;